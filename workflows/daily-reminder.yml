name: Daily Contribution Reminder

on:
  schedule:
    - cron: "0 9 * * *" # Every day at 9 AM UTC
  workflow_dispatch: # allows manual run

jobs:
  reminder:
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - name: Send reminder
        uses: peter-evans/create-issue@v5
        with:
          title: "Daily Contribution Reminder - ${{ github.run_id }}"
          body: |
            👋 **Hello team!**  
            It's time to contribute for today 🚀  

            Please make sure to:
            - Commit your changes.
            - Review open pull requests.
            - Push progress updates.

            Contributors:  
            @user1 @user2 @user3  
            
            Keep it up 💪
          labels: reminder
